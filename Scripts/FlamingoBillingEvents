SELECT
  catalog_id,
  PVID,
  CAST(created_at AS DATE) AS created_at,
  output_data:additional_variant_attribute::string
FROM SCALE_CRAWLER.PUBLIC.OUTPUTVARIANTS
WHERE enrichment_version IS NOT NULL
  AND customer = 'flamingo'
  AND submission_state = 'submission'
  AND CAST(enrichment_version AS DATE) > '2023-02-28'
  --AND AdditionalVariantAttribute IS NOT NULL
LIMIT 100;